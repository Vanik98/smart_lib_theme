# example/pubspec.yaml

name: smart_lib_theme_example
description: Example app for smart_lib_theme package.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  smart_lib_theme:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
