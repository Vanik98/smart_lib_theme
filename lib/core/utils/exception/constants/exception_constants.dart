abstract class ThemeExceptionConstants {
  static const initErrorMessage = "Failed to initialize AppThemeBuilder. Please ensure proper setup by awaiting AppThemeManager.init() before instantiating this widget. Verify that the theme manager is correctly initialized and configured in your application's initialization flow.";
}
